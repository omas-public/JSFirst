{
  "product function snippet": {
    "scope": "javascript,javascriptreact",
    "prefix": "productfun",
    "body": [
      "const product = (...iters) => iters.reduce(",
      "    (a, b) => a.flatMap(d => b.map(e => [d, e].flat())))"
    ],
    "description": "product function"
  }
}